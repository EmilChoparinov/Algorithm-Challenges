function unionSetUnsorted(a, b) {
    if (a instanceof Array && b instanceof Array) {
        let ai = 0, bi = 0, arr = [];
        while (ai < a.length && bi < b.length) {
            
        }
    }
}
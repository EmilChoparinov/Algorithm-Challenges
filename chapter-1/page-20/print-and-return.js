/**
 * Print the first value, return the second
 * @param {Array} arr array of two numbers
 */
function printAndReturn(arr){
    console.log(arr[0]);
    return arr[1];
}

console.log(printAndReturn([1,2]))
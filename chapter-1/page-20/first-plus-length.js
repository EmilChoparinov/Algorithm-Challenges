/**
 * Returns the sum of the first value in the array plus the array's length.
 * @param {Number} arr arr of length > 1
 * @returns {Object}
 */
function firstPlusLength(arr){
    return arr[0]+arr.length;
}

console.log(firstPlusLength([1,2,3]))
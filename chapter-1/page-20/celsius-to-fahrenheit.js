/**
 * Convert from celsius to fahrenheit
 * @param {Number} cDegrees degress in celcius
 * @returns {Number} fahrenheit degress
 */
function celsiusToFahrenheit(cDegrees){
    return (cDegrees * 9/5) + 32;
}